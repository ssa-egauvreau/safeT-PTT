<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".RadioApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SecurityRadio">
        <activity
            android:name=".DisplayRouterActivity"
            android:exported="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:resizeableActivity="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.SecurityRadio" />

        <service
            android:name=".device.RadioPresenceService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <!-- Subtype label for FOREGROUND_SERVICE_TYPE_SPECIAL_USE policy review. -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="background_ptt_hardware_routing_coordination" />
        </service>

        <!-- Not directBootAware on purpose: this app keeps its state in credential-encrypted
             storage, so it must not be launched before the user unlocks after a reboot. Listening
             for LOCKED_BOOT_COMPLETED (direct boot) here crashed the process on first boot
             (TM7 / Android 11 "safeT stopped working"). BOOT_COMPLETED + USER_PRESENT fire after
             unlock, which is the earliest point the radio can safely start. -->
        <receiver
            android:name=".device.RadioBootReceiver"
            android:exported="true">
            <intent-filter android:priority="998">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>

        <service
            android:name=".device.InricoHardwareService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".device.InricoBroadcastReceiver"
            android:exported="true">
            <intent-filter android:priority="999">
                <action android:name="com.zello.ptt.down" />
                <action android:name="com.zello.ptt.up" />
                <action android:name="android.intent.action.PTT.down" />
                <action android:name="android.intent.action.PTT.up" />
                <action android:name="com.android.ptt.down" />
                <action android:name="com.android.ptt.up" />
                <action android:name="com.zello.intent.channelUp" />
                <action android:name="com.zello.intent.channelDown" />
                <action android:name="com.android.intent.action.CHANNEL_UP" />
                <action android:name="com.android.intent.action.CHANNEL_DOWN" />
            </intent-filter>
        </receiver>

        <!-- PackageInstaller session status callbacks for OTA self-updates.
             Not exported: only ever invoked via our own PendingIntent. -->
        <receiver
            android:name=".device.AppUpdateInstallReceiver"
            android:exported="false" />
    </application>

</manifest>
