# Your real config holds passwords / SafeT credentials — never commit it.
config/system.json
config/talkgroups.csv

# Everything generate.mjs produces from your config.
generated/

# Local Icecast/trunk-recorder runtime state.
trunk-recorder/config.json
trunk-recorder/talkgroups.csv
icecast/icecast.xml
*.log
.env

# Windows launcher's saved settings (may contain the repo URL).
windows/launcher.config.json
